mixin form-change-multi(action)
    form(
            action = action
            method = 'POST'
            form-change-multi
        )
            .d-flex.align-items-start
                .form-group.me-2
                    select(name = 'type' class = 'form-control')
                        option(disabled selected) Choose action
                        option(value = 'active') Active
                        option(value = 'inactive') Inactive
                        option(value = 'delete-all') Delete all
                        option(value = 'change-position') Change position
                .form-group.me-2
                    input(
                        type = 'text'
                        name = 'ids'
                        value = ''
                        class = 'form-control d-none'
                    )
                button(type = 'submit', class = 'btn btn-primary' style = 'height: 38px') Apply 